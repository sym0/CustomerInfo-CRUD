source 'https://rubygems.org'

ruby '>=2.6.6'

gem 'activerecord', '4.2.11'
gem 'byebug'
gem 'sqlite3', '1.3.11'
gem 'rspec', '~> 3.0'
# Only uncomment the next line if you need to re-generate the fake data. Note that
# re-generating the fake data WILL BREAK THE RSPEC TESTS.
# gem 'faker'
gem 'guard-rspec'
#出现版本错误，ubuntu20默认安装的3.0.*，此版本在运行bundle exec rspec spec/activerecord_practice_spec.rb回报错
#sudo gem install bigdecimal --version 1.3.5
gem 'bigdecimal', '1.3.5'

